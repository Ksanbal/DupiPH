import 'package:flutter/material.dart';

class InfoPage_Cop extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('회사소개'),
      ),
      body: Center(
        child: Text('회사 소개입니다.'),
      ),
    );
  }
}
